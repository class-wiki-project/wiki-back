<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wings.mywiki.dao.mybatis.mapper.UsersMapper">
  <cache />
  
  <resultMap id="resultMap_users" type="com.wings.mywiki.model.UsersVO">
	<result property="userId" column="user_id" />
	<result property="email" column="email" />
	<result property="studentName" column="student_name" />
	<result property="studentNumber" column="student_number" />
	<result property="auth" column="auth" />
	<result property="univName" column="univ_name" />
</resultMap>
  <resultMap id="resultMap_users2" type="com.wings.mywiki.model.UserOutVO">
	<result property="userId" column="user_id" />
	<result property="email" column="email" />
	<result property="studentName" column="student_name" />
	<result property="studentNumber" column="student_number" />
	<result property="univName" column="univ_name" />
</resultMap>
  
  <insert id="insert">
  INSERT INTO users (user_id, email, password, student_name,
  student_number, auth, univ_name) values (#{userId}, 
  #{email}, #{password}, #{studentName}, #{studentNumber}, 1, 
  #{univName})
  </insert>
  <select id="selectAll" resultMap="resultMap_users">
  	select user_id, email, student_name, student_number, univ_name from 
  	users
  </select>
  
  <select id="selectOne" resultMap="resultMap_users2">
  	select user_id, email, student_name, student_number, univ_name from users 
  	where user_id=#{userId}
  </select>
  
  <update id="update">
  	update users set email=#{email}, password=#{password}, 
  	student_name=#{studentName}, student_number=#{studentNumber}, 
  	univ_name=#{univName} where user_id=#{userId}
  </update>
  
  <delete id="delete">
  	delete from users where user_id=#{userId}
  </delete>
  
  <select id="checkId" resultType="int">
		SELECT COUNT(*)
		FROM users
		WHERE email=#{email}
	</select>
  <select id="checkLogin" resultMap="resultMap_users">
		SELECT *
		FROM users
		WHERE email=#{email}
	</select>
</mapper>